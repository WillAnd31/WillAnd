{
	"apps": [
		{
			"name": "willand",
			"script": "server/server.js",
			"env_staging": {
				"NODE_ENV": "staging"
			}
		}
	],
	"deploy": {
		"staging": {
			"key": "./space-rocks.pem",
			"user": "node",
			"host": "52.36.86.214",
			"repo": "git@github.com:WillAnd31/WillAnd.git",
			"ref": "origin/master",
			"path": "/home/ubuntu/WillAnd",
			"post-deploy": "npm install && cd ./client/ && typings install && cd ./../server/ && typings install && cd ./../ && pm2 startOrRestart ecosystem.json --env staging"
		}
	}
}
